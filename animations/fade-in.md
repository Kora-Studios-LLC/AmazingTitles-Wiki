# FADE_IN

`FADE_IN` reveals text progressively from left to right, preserving existing color formatting.

## Arguments

- none

## Example

```text
/at sendAnimation all p:TITLE,d:4,fps:20 FADE_IN &dWelcome back
```
