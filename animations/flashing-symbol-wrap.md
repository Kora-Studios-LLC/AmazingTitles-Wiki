# FLASHING_SYMBOL_WRAP

`FLASHING_SYMBOL_WRAP` alternates between plain text and a wrapped version surrounded by a symbol.

## Arguments

- `Symbol(Text)`

## Example

```text
/at sendAnimation all p:TITLE,d:4,fps:8 FLASHING_SYMBOL_WRAP * &eEvent starting
```
