# FROM_BOTH_SIDES

`FROM_BOTH_SIDES` simulates a center join by separating the text and closing the gap over time.

## Arguments

- none

## Example

```text
/at sendAnimation all p:TITLE,d:4,fps:18 FROM_BOTH_SIDES &dMatch found
```
